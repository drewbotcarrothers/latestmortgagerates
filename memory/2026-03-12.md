# 2026-03-12 - Mortgage Rate Scraper Updates

## Summary
Successfully fixed and deployed the mortgage rate scraper for latestmortgagerates.ca. Site now auto-updates twice daily with current rates from 31 Canadian lenders.

## Problem Resolution

### Issue 1: FTP Deploy Directory
- **Problem**: Workflow deploying to `/public_html/` but Hostinger uses root
- **Fix**: Changed `server-dir: /public_html/` → `server-dir: ./`
- **Commit**: `14df8e8`

### Issue 2: Build Output Path  
- **Problem**: `next.config.ts` creates `dist/` at repo root, workflow looked for `app/dist/`
- **Error**: `dist/build-info.txt: No such file or directory`
- **Fix**: Updated build step with proper `working-directory`, changed `local-dir: ./app/dist/` → `local-dir: ./dist/`
- **Commits**: `b947866`, `dbb65bd`, `50adf4d`

### Issue 3: Workflow Permissions
- **Problem**: GitHub Actions couldn't push commits (403 error)
- **Fix**: Added `permissions: contents: write` to workflow  
- **Commit**: `11798ca`

### Issue 4: Git Race Condition
- **Problem**: `git pull --rebase` failing with "unstaged changes" after commit
- **Fix**: Removed `git pull --rebase` step entirely
- **Commit**: `542ab85`

## Workflow Refactoring

**Commit**: `50adf4d`

Refactored all GitHub Actions workflows for monorepo structure:

| Workflow | Changes |
|----------|---------|
| `scrape-and-deploy.yml` | Uses `working-directory: app` for npm, `working-directory: scraping` for Python |
| `ci-cd.yml` | Fixed paths with `working-directory: app` |
| `post-to-social.yml` | Fixed paths with `working-directory: app` |
| `scrape-and-deploy-complex.yml` | Archived to `.old` |

## Final Status - DEPLOYED ✅

**Date**: 2026-03-12 07:35 EDT

- ✅ 213 rates from 31 lenders
- ✅ True North Mortgage: 18 rates (3.94% 5yr fixed insured, 3.80% 5yr variable insured)
- ✅ Site live: https://latestmortgagerates.ca
- ✅ Schedule: 6 AM and 6 PM EST daily (`0 11,23 * * *`)

## Key Configuration

**Build Output**: `dist/` at repo root (configured in `next.config.ts`)

**Deploy**: `local-dir: ./dist/` → `server-dir: ./` (Hostinger root)

**Secrets**: `SFTP_HOST`, `SFTP_USERNAME`, `SFTP_PASSWORD`
